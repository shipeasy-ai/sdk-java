# Self-contained example — no external services, no Shipeasy SDK wired yet.
spring.application.name=shipeasy-guide-example
server.port=8080

# Serve a fresh template on every request while you tweak the guide locally.
spring.thymeleaf.cache=false
