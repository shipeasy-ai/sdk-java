# Auto-register globally-declared JUnit 5 extensions (ServiceLoader).
# This picks up InertInternalReportExtension, which keeps the InternalReport
# self-monitoring channel inert (ingest key reset to the placeholder) before every
# test in the suite, so no test can fire a real POST to api.shipeasy.ai/collect.
junit.jupiter.extensions.autodetection.enabled=true
